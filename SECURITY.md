# Security

The scanner operates locally and has no network dependency. Do not add telemetry or remote content submission without an explicit design review and user opt-in.

Report security issues privately to the repository maintainer rather than posting exploit details publicly.
